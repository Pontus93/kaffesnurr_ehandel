import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    main: {
        marginTop: "10rem",
        maxWidth: '100%',
        color: 'black',
        margin: 'auto',
        paddingBottom: '50px',
    }
}));
